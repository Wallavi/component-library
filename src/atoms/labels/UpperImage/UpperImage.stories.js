import React from "react";
import UpperImage from "./index";

export default {
  component: UpperImage,
  title: "Atoms/Labels/UpperImage",
};

const Template = (args) => <UpperImage {...args} />;

export const Default = Template.bind({});
Default.args = {};
