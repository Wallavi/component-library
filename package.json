{
  "name": "component-library",
  "version": "1.0.0",
  "description": "component library root",
  "main": "library/dist/index.js",
  "scripts": {
    "install-library": "cd library && rm -rf node_modules && rm -rf dist && npm install",
    "create-distribution": "cd library && rm -rf dist && npm run prepare",
    "install-storybook": "cd storybook && rm -rf node_modules && npm install",
    "storybook": "cd storybook && npm run storybook"
  },
  "author": "Josias Ortiz",
  "license": "ISC"
}
